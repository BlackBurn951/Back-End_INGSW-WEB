package com.example.progettowebtest.Controller;
/*
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;

@Controller
public class NotificheeController {
    @GetMapping("/tuaPagina")
    public String tuaPagina(Model model) {
        // Aggiungi qui la logica per ottenere il numero di notifiche non lette e le notifiche
        int numeroNotificheNonLette = ...;
        List<Notifica> notifiche = ...;

        model.addAttribute("numeroNotificheNonLette", numeroNotificheNonLette);
        model.addAttribute("notifiche", notifiche);

        return "tuaPagina";
    }
}
*/